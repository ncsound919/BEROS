Overlay Open Source\AutoCoder-Forge-Basic\templates\build_csharp_exe.bat.j2
@echo off
REM Build script for Carlton Model executable
REM Generated by AutoCoder
REM
REM Template variables:
REM   - project_file: str (the .csproj file name)
REM   - output_dir: str (the publish output directory)
REM   - exe_name: str (the name of the exe)

echo Building Carlton Model as standalone executable...

REM Restore packages
dotnet restore {{ project_file }}

REM Publish as self-contained exe for Windows x64
dotnet publish {{ project_file }} -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:PublishTrimmed=true

echo Build complete. Executable is in {{ output_dir }}\{{ exe_name }}

pause
