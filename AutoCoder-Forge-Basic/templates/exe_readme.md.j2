# Overlay Cheetah - Portable Code Generation Tool

**Version:** {{ version }}
**Executable:** {{ exe_name }}

---

## What is Overlay Cheetah?

Overlay Cheetah is a **portable, standalone code generation system** that scaffolds production-ready projects in seconds. It automatically generates:

- ✅ Backend API with FastAPI (Python)
- ✅ Frontend UI with React + TypeScript
- ✅ Complete test suites (pytest + vitest)
- ✅ Docker deployment stack (Compose + Dockerfiles)
- ✅ Health monitoring endpoints
- ✅ CI/CD configurations
- ✅ Documentation and project automation

**No Python installation required!** This `.exe` contains everything you need.

---

## Quick Start (3 Steps)

### Step 1: Run the Executable

Double-click `{{ exe_name }}` or run from command line:

```cmd
{{ exe_name }}
```

This will:
- Install Overlay Cheetah into your current directory
- Generate all project files automatically
- Create a complete, deployment-ready codebase

**Advanced:** Specify a target directory:

```cmd
{{ exe_name }} C:\Projects\MyNewProject
```

### Step 2: Install Dependencies

After generation completes, install project dependencies:

**Backend:**
```cmd
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```

**Frontend:**
```cmd
cd agent-ui
npm install
```

### Step 3: Run Your Project

**Option A - Development Mode:**
```cmd
REM Terminal 1: Backend
venv\Scripts\activate
uvicorn apps.api.main:app --reload

REM Terminal 2: Frontend
cd agent-ui
npm run dev
```

**Option B - Docker (Recommended):**
```cmd
docker-compose up --build
```

Access your application:
- Backend API: http://localhost:8000
- API Documentation: http://localhost:8000/docs
- Frontend UI: http://localhost:5173
- Health Check: http://localhost:8000/api/v1/health

---

## What Gets Generated?

Overlay Cheetah creates a complete project structure:

```
YourProject/
├── apps/
│   └── api/
│       ├── routers/          # API endpoints
│       │   ├── health.py     # Health checks
│       │   ├── session.py    # Session management
│       │   ├── agent.py      # Agent operations
│       │   ├── engine.py     # Protocol engine
│       │   └── protocols.py  # Protocol management
│       ├── services/         # Business logic
│       └── tests/            # Backend tests (25+ test cases)
│           ├── conftest.py
│           ├── test_queue.py
│           └── test_session.py
├── agent-ui/                 # React frontend
│   ├── src/
│   │   ├── components/       # UI components
│   │   ├── __tests__/        # Frontend tests
│   │   └── mocks/            # MSW mocks
│   └── package.json
├── config/
│   ├── protocols/            # Protocol definitions
│   └── routes.json           # Routing config
├── docker-compose.yml        # Docker orchestration
├── Dockerfile.backend        # Backend container
├── Dockerfile.frontend       # Frontend container
├── Makefile                  # Automation commands
├── pytest.ini                # Test configuration
├── requirements.txt          # Python dependencies
├── README.md                 # Project documentation
└── QUICKSTART.md             # Quick start guide
```

---

## System Requirements

### For Running the Executable:
- ✅ **Windows 10/11** (64-bit)
- ✅ **No Python required!** (bundled in exe)

### For Running the Generated Project:
- **Python 3.10+** (for backend)
- **Node.js 18+** (for frontend)
- **Redis** (for session storage)
  - Install locally or use Docker: `docker run -d -p 6379:6379 redis:alpine`
- **Docker** (optional, for containerized deployment)

---

## Using Overlay Cheetah with Your IDE

### Visual Studio Code
1. Open your project folder in VS Code
2. Open integrated terminal (Ctrl+`)
3. Run: `{{ exe_name }}`
4. Wait for generation to complete
5. Start coding!

### JetBrains IDEs (PyCharm, WebStorm)
1. Open project in IDE
2. Open terminal panel
3. Run: `{{ exe_name }}`
4. Reload project to see generated files

### Zed, Sublime, Vim, etc.
1. Navigate to project directory
2. Run: `{{ exe_name }}`
3. Refresh file tree

---

## Common Use Cases

### Starting a New Project
```cmd
mkdir MyNewProject
cd MyNewProject
{{ exe_name }}
```

### Adding to Existing Project
```cmd
cd ExistingProject
{{ exe_name }}
```
*Note: Existing files will prompt for overwrite confirmation*

### Prototyping Quickly
```cmd
mkdir prototype && cd prototype
{{ exe_name }}
docker-compose up
```
*Full stack running in under 5 minutes!*

---

## Generated Files Explained

### Backend Components

**`apps/api/routers/health.py`**
- Health check endpoints
- Redis connectivity monitoring
- Kubernetes-style liveness/readiness probes

**`apps/api/routers/session.py`**
- Session CRUD operations
- Log management
- Redis-backed persistence

**`apps/api/tests/`**
- 25+ comprehensive test cases
- Fixtures and mocks included
- pytest configuration ready

### Frontend Components

**`agent-ui/src/components/Inbox.tsx`**
- Real-time agent inbox
- WebSocket integration
- Session management UI

**`agent-ui/src/__tests__/`**
- Vitest test suite
- React Testing Library setup
- MSW for API mocking

### Deployment

**`docker-compose.yml`**
- Multi-service orchestration
- Redis + Backend + Frontend
- Health checks configured

**`Makefile`**
- `make install` - Install dependencies
- `make test` - Run all tests
- `make build` - Build Docker images
- `make up` - Start services
- `make down` - Stop services

---

## Troubleshooting

### "Python not found" after generation
**Solution:** The exe bundles Python for running the generator only. Your project still needs Python installed to run the backend.

Install Python: https://www.python.org/downloads/

### "Permission denied" error
**Solution:** Run command prompt as Administrator or move to a directory where you have write permissions.

### "Files already exist" warning
**Solution:**
- Choose "n" to keep existing files
- Choose "y" to overwrite with generated files
- Backup important files before overwriting

### Generation seems stuck
**Solution:**
- Wait up to 60 seconds (first run installs dependencies)
- Check your antivirus isn't blocking the exe
- Try running from command prompt to see detailed output

### Docker not starting
**Solution:**
- Ensure Redis is running: `docker run -d -p 6379:6379 redis:alpine`
- Check ports 8000 and 5173 are available
- Run `docker-compose logs` for error details

---

## Tips for AI Assistants (Copilot, ChatGPT, etc.)

When working with a project generated by Overlay Cheetah:

1. **Reference the existing structure** - All routes, tests, and configs follow consistent patterns
2. **Extend, don't replace** - Add new routers/components alongside generated ones
3. **Follow the patterns** - Use generated code as templates for new features
4. **Tests are ready** - Run `pytest` or `npm test` to verify your changes

### Example Prompts:

**"Add a new API endpoint for user management"**
→ AI will follow the pattern in `apps/api/routers/session.py`

**"Create tests for the new endpoint"**
→ AI will follow the pattern in `apps/api/tests/test_session.py`

**"Add a new React component for user profile"**
→ AI will follow the pattern in `agent-ui/src/components/Inbox.tsx`

---

## Speed Comparison

### Manual Development Time:
- Backend API setup: **2-3 hours**
- Frontend scaffolding: **2-3 hours**
- Test infrastructure: **2-4 hours**
- Docker setup: **1-2 hours**
- Documentation: **1-2 hours**
- **Total: 8-14 hours**

### With Overlay Cheetah:
- **20 seconds** (generation time)
- **5 minutes** (dependency installation)
- **Total: ~6 minutes**

**Result: 80-140x faster** ⚡

---

## What Makes Overlay Cheetah Different?

| Feature | Overlay Cheetah | Manual Setup | Other Generators |
|---------|----------------|--------------|------------------|
| **Speed** | 20 seconds | 8-14 hours | 30-60 minutes |
| **Completeness** | 95%+ ready | Varies | 60-80% ready |
| **Tests Included** | ✅ 25+ tests | ❌ Usually none | ⚠️ Basic only |
| **Docker Ready** | ✅ Full stack | ❌ Manual setup | ⚠️ Partial |
| **Documentation** | ✅ Complete | ❌ None | ⚠️ Minimal |
| **Portable** | ✅ Single exe | N/A | ❌ Needs runtime |
| **No Dependencies** | ✅ Standalone | N/A | ❌ Needs Node/Python |

---

## Distribution & Licensing

**Overlay Cheetah** is a proprietary code generation tool.

### You CAN:
- ✅ Use generated code in commercial projects
- ✅ Modify generated code as needed
- ✅ Share the exe within your team
- ✅ Run unlimited times on unlimited projects

### You CANNOT:
- ❌ Reverse engineer the exe
- ❌ Sell or redistribute the exe publicly
- ❌ Claim ownership of the Overlay Cheetah system

**Generated code is yours** - no attribution required.

---

## Support & Updates

### Getting Help:
- Check generated `README.md` in your project
- Check generated `QUICKSTART.md` for setup steps
- Review `CODE_AUDIT.md` for code quality insights

### Version Info:
- Current Version: {{ version }}
- Release Date: October 2024
- Compatibility: Windows 10/11, Python 3.10+, Node.js 18+

---

## Advanced Usage

### Custom Templates
After generation, you can modify templates in `AutoCoder-Forge-Basic/templates/` and re-run to regenerate with your changes.

### Environment Variables
Set these before running {{ exe_name }}:
- `OVERLAY_CHEETAH_TARGET` - Override target directory
- `OVERLAY_CHEETAH_SKIP_DEPS` - Skip dependency installation

### Batch Generation
```cmd
for /d %i in (project1 project2 project3) do (
    cd %i && {{ exe_name }} && cd ..
)
```

---

## Quick Command Reference

```cmd
REM Run in current directory
{{ exe_name }}

REM Run in specific directory
{{ exe_name }} C:\Projects\MyApp

REM After generation - install deps
python -m venv venv && venv\Scripts\activate && pip install -r requirements.txt
cd agent-ui && npm install

REM Run tests
pytest apps/api/tests/ -v
cd agent-ui && npm run test:run

REM Start with Docker
docker-compose up --build

REM Start development mode
venv\Scripts\activate && uvicorn apps.api.main:app --reload
cd agent-ui && npm run dev

REM Build frontend for production
cd agent-ui && npm run build

REM Use Makefile shortcuts
make install && make test && make up
```

---

## Success Checklist

After running Overlay Cheetah, verify:

- [ ] `apps/api/` directory exists with routers
- [ ] `agent-ui/` directory exists with React app
- [ ] `docker-compose.yml` created
- [ ] `pytest.ini` created
- [ ] `Makefile` created
- [ ] `README.md` and docs created
- [ ] Tests pass: `pytest apps/api/tests/ -v`
- [ ] Docker starts: `docker-compose up`
- [ ] Health endpoint responds: http://localhost:8000/api/v1/health

---

**🐆 Happy Coding with Overlay Cheetah!**

*Generated in seconds. Production-ready immediately.*

---

**Questions or Issues?**
Review the generated `README.md` in your project for detailed documentation.
