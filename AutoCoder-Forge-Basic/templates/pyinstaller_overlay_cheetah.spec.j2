# -*- mode: python ; coding: utf-8 -*-
"""
PyInstaller spec file for Overlay Cheetah
Generated by AutoCoder

Template variables:
  - app_name: str (default "OverlayCheetah")
  - script_name: str (default "overlay_cheetah_launcher.py")
  - icon_file: str (optional)
  - version: str (default "1.0.0")
  - company_name: str (default "Overlay Cheetah")
  - file_description: str
  - console: bool (default True)
  - onefile: bool (default True)
"""

import os
from PyInstaller.utils.hooks import collect_data_files, collect_submodules

block_cipher = None

# Collect AutoCoder runtime and templates
datas = []

# Add AutoCoder-Forge-Basic directory
autocoder_base = 'AutoCoder-Forge-Basic'
if os.path.exists(autocoder_base):
    datas.append((os.path.join(autocoder_base, 'autocoder.py'), 'AutoCoder-Forge-Basic'))
    datas.append((os.path.join(autocoder_base, 'requirements.txt'), 'AutoCoder-Forge-Basic'))

    # Add templates
    templates_dir = os.path.join(autocoder_base, 'templates')
    if os.path.exists(templates_dir):
        datas.append((templates_dir, 'templates'))

    # Add tasks
    tasks_dir = os.path.join(autocoder_base, 'tasks')
    if os.path.exists(tasks_dir):
        datas.append((tasks_dir, 'tasks'))

# Hidden imports needed by AutoCoder
hiddenimports = [
    'jinja2',
    'jinja2.ext',
    'yaml',
    'watchdog',
    'watchdog.observers',
    'watchdog.events',
]

a = Analysis(
    ['{{ script_name }}'],
    pathex=[],
    binaries=[],
    datas=datas,
    hiddenimports=hiddenimports,
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[
        'matplotlib',
        'numpy',
        'pandas',
        'PIL',
        'tkinter',
        'unittest',
    ],
    win_no_prefer_redirects=False,
    win_private_assemblies=False,
    cipher=block_cipher,
    noarchive=False,
)

pyz = PYZ(a.pure, a.zipped_data, cipher=block_cipher)

{% if onefile %}
exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.zipfiles,
    a.datas,
    [],
    name='{{ app_name }}',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console={{ 'True' if console else 'False' }},
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    {% if icon_file %}
    icon='{{ icon_file }}',
    {% endif %}
    version_info={
        'version': '{{ version }}',
        'company_name': '{{ company_name }}',
        'file_description': '{{ file_description }}',
        'internal_name': '{{ app_name }}',
        'legal_copyright': 'Copyright (c) 2024',
        'original_filename': '{{ app_name }}.exe',
        'product_name': '{{ app_name }}',
        'product_version': '{{ version }}',
    }
)
{% else %}
exe = EXE(
    pyz,
    a.scripts,
    [],
    exclude_binaries=True,
    name='{{ app_name }}',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    console={{ 'True' if console else 'False' }},
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    {% if icon_file %}
    icon='{{ icon_file }}',
    {% endif %}
)

coll = COLLECT(
    exe,
    a.binaries,
    a.zipfiles,
    a.datas,
    strip=False,
    upx=True,
    upx_exclude=[],
    name='{{ app_name }}',
)
{% endif %}
