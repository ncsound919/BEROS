# -*- mode: python ; coding: utf-8 -*-
"""
PyInstaller spec file for Pygame Game
Generated by AutoCoder

Template variables:
  - app_name: str (default "PygameGame")
  - script_name: str (default "main.py")
  - icon_file: str (optional)
  - version: str (default "1.0.0")
  - company_name: str (default "AutoCoder Games")
  - file_description: str
  - console: bool (default False)
  - onefile: bool (default True)
"""

import os
from PyInstaller.utils.hooks import collect_data_files, collect_submodules

block_cipher = None

# Collect Pygame data files
datas = collect_data_files('pygame')

# Hidden imports for Pygame
hiddenimports = collect_submodules('pygame')

a = Analysis(
    ['main.py'],
    pathex=['{{ game_dir }}'],
    binaries=[],
    datas=datas,
    hiddenimports=hiddenimports,
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    win_no_prefer_redirects=False,
    win_private_assemblies=False,
    cipher=block_cipher,
    noarchive=False,
)

pyz = PYZ(a.pure, a.zipped_data, cipher=block_cipher)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.zipfiles,
    a.datas,
    [],
    name='{{ app_name }}',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console={{ 'True' if console else 'False' }},
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    {% if icon_file %}
    icon='{{ icon_file }}',
    {% endif %}
    version_info={
        'version': '{{ version }}',
        'company_name': '{{ company_name }}',
        'file_description': '{{ file_description }}',
        'internal_name': '{{ app_name }}',
        'legal_copyright': 'Copyright (c) 2024',
        'original_filename': '{{ app_name }}.exe',
        'product_name': '{{ app_name }}',
        'product_version': '{{ version }}',
    }
)
